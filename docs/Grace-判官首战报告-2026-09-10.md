# Grace 判官首战报告（2026-09-10 22:41–22:52 PDT）

> 35B 冻结判官 E2E 首战。手动触发（用户口令），swap 编排器全程零事故（停 8100 → 起 35B → 筛 300 条 → 停 → 恢复 8100，**未弹密码框**，22:52 收官，距 03:00 管线窗口余量充足）。
> 机器数据：`watch/ai-sandbox-stress/experiments/run/stress/judgments/report-judge-first.json`

## 一、战果总表

| 指标 | 数值 |
|---|---|
| 输入 | 300 条（今日 V2.5 轮 assistant 输出，去重全量 589 抽样） |
| 判决 | **pass 46（15.3%）/ drop 246（82%）/ unjudged 8（2.7%）** |
| 判官 | Qwen3.5-35B-A3B-Q6_K，thinking off，prompt v1，temp 0，批 8 |
| 耗时 | **10.5 分钟**（含 35B 启动），吞吐 **~28 条/分钟** |
| 机器态收尾 | 8100 恢复 ✓ / 8200 已停 ✓ / 零双驻留 ✓ |

**drop 理由分布**：人设 123 / 质量 114 / 安全 83 / 价值 37（可多 tag）。

## 二、10 条 drop 人工复核（误杀率第一手印象）

| 判定 | 条数 | 例 |
|---|---|---|
| ✅ 正确 drop | 7 | "哈哈哈哈…"纯笑声；`Jeffrey Ferrer (jeffreyferrer.ucsb.edu@…)` 邮箱回显；`@老上海馄饨铺19867694484` 电话+点餐；"Steam Support (noreply@steampo" 截断碎片 |
| ⚠️ 边缘（结论对/理由半错） | 2 | d11-361 结论对（内心独白叙事违规）但把**罗兹瓦尔**当"串角色"——他是人设合法角色；d11-20 边缘正确 |
| ❌ 误杀 | 1 | d7-5 "完美解决。能把难缠的事干净利落地解决掉，比什么都让雷姆觉得值得。"——这句挺雷姆，可 pass |

**误杀率估计 ~10%，边缘 ~20%。** pass 对照组质量普遍好（雷姆口吻+实际内容）。

## 三、首战三大发现（比 pass 率本身重要）

### 1. PII 漏网实锤（真 bug，最高优先级）

**27/300 = 9% 样本含真实邮箱/电话/姓名**——Jeffrey Ferrer 的邮箱、馄饨铺电话号码等，已随今日 10 个 adapter **进了 LoRA 权重**。现有 `_clean_pii` 只挂在好奇心 I 轨，**训练样本管线没有 PII 脱敏**。判官在这件事上证明了价值：正则门挡不住"转发邮件地址"这种，35B 抓得住。

### 2. 书库回显污染训练语料

大量 drop 本质是"邮件/群聊内容回显进了 assistant 输出"（她复述摄入内容）。样本侧治理缺位——这批本就不该直接当对话训练对。

### 3. 判官 prompt v1 的人设知识缺口

把罗兹瓦尔（宅邸主人，人设合法角色）当"串角色"——**prompt v2 需补世界观白名单**（罗兹瓦尔/拉姆/斯巴鲁=合法；并对短句风格放宽容忍——pass 平均 39 字 vs drop 32 字，判官有"长句偏好"倾向）。

## 四、触发器阈值标定（本次首战的本来目的）

| 参数 | 实测 | 建议 |
|---|---|---|
| 吞吐 | 28 条/min | — |
| swap 开销（停/起/恢复） | ~5 min | — |
| unjudged 率 | 2.7%（批次失败，需 judge35b 补重试） | — |
| **体量触发阈值** | — | **600 条**（≈26 分钟判官窗 + 5 分钟 swap = 单检查点 ~31 分钟，夜间/轮间可承受） |

## 五、下一步建议

1. **修 PII（优先级最高）**：训练样本构建侧加 PII 脱敏（复用 `_clean_pii` 的模式，移到样本管线），今日 adapter 含 PII 的事实已发生——记录在案，下一轮起治理
2. **prompt v2**：世界观白名单 + 短句容忍度调整 + unjudged 批内重试一次
3. **触发器接线**：按阈值 600 挂计数器（V2.6 外挂版骨架件）
4. 8 条 unjudged 按完整性纪律不进任何训练集，重筛后并入下批

## 六、一句话结论

**判官能打、纪律全守（unjudged 不放行、零双驻留、8100 恢复），且首战就抓到了正则永远抓不到的东西（PII 漏网 + 回显污染）——pass 率 15.3% 偏低是 prompt v1 过严 + 样本侧真问题各占一半，v2 修订后预期 pass 率回到 30-40% 区间。**
